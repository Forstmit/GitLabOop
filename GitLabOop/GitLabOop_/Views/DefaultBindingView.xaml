<UserControl x:Class="GitLabOop.Views.DefaultBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <Border Style="{StaticResource SurfaceCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_Header]}"
                               Style="{StaticResource SectionTitleStyle}" />
                    <TextBlock Margin="0,10,0,0"
                               Text="{Binding Source={StaticResource Loc}, Path=[Default_Description]}"
                               Style="{StaticResource InfoTextStyle}" />
                </StackPanel>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Margin="0,0,10,0"
                        Style="{StaticResource SurfaceCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_DirectCardTitle]}"
                                   Style="{StaticResource CardTitleStyle}" />
                        <TextBlock Margin="0,8,0,18"
                                   Text="{Binding Source={StaticResource Loc}, Path=[Default_DirectCardDescription]}"
                                   Style="{StaticResource InfoTextStyle}" />

                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_SourceLabel]}"
                                   Style="{StaticResource InputLabelStyle}" />
                        <Slider x:Name="DirectDefaultSlider"
                                Minimum="0"
                                Maximum="100"
                                Value="56" />

                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_TextBoxLabel]}"
                                   Style="{StaticResource InputLabelStyle}" />
                        <TextBox Text="{Binding ElementName=DirectDefaultSlider, Path=Value, StringFormat={}{0:F0}}" />

                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_TextBlockLabel]}"
                                   Style="{StaticResource InputLabelStyle}" />
                        <TextBlock Text="{Binding ElementName=DirectDefaultSlider, Path=Value, StringFormat={}{0:F0}%}"
                                   Style="{StaticResource InfoTextStyle}" />

                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_ProgressLabel]}"
                                   Style="{StaticResource InputLabelStyle}" />
                        <ProgressBar Minimum="0"
                                     Maximum="100"
                                     Value="{Binding ElementName=DirectDefaultSlider, Path=Value}" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1"
                        Margin="10,0,0,0"
                        Style="{StaticResource SurfaceCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_ViewModelCardTitle]}"
                                   Style="{StaticResource CardTitleStyle}" />
                        <TextBlock Margin="0,8,0,18"
                                   Text="{Binding Source={StaticResource Loc}, Path=[Default_ViewModelCardDescription]}"
                                   Style="{StaticResource InfoTextStyle}" />

                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_BlockNameLabel]}"
                                   Style="{StaticResource InputLabelStyle}" />
                        <TextBox Text="{Binding PresenterName}" />

                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_DescriptionLabel]}"
                                   Style="{StaticResource InputLabelStyle}" />
                        <TextBox Text="{Binding Notes}" />

                        <CheckBox Content="{Binding Source={StaticResource Loc}, Path=[Default_PinCard]}"
                                  IsChecked="{Binding IsPinned}" />

                        <TextBlock Text="{Binding Source={StaticResource Loc}, Path=[Default_AccentOpacityLabel]}"
                                   Style="{StaticResource InputLabelStyle}" />
                        <Slider Minimum="10"
                                Maximum="100"
                                Value="{Binding AccentOpacity}" />

                        <Border Height="88"
                                CornerRadius="18"
                                Background="{StaticResource AccentBrush}"
                                Opacity="{Binding AccentOpacityRatio}">
                            <TextBlock VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Foreground="White"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Text="{Binding PresenterName}" />
                        </Border>

                        <Border Margin="0,16,0,0"
                                Style="{StaticResource SummaryBadgeStyle}">
                            <TextBlock Text="{Binding Summary}"
                                       Style="{StaticResource InfoTextStyle}" />
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
